<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

---

# AGENTS.md — expo-auto-planner
> Auto-generated by the dev pipeline scanner. Do not edit manually (except the sections above/below marked as project-owned).
> Last updated: 2026-07-26 (delta: 場地規劃器補件 — src/components/venue/ + src/lib/venue/ 分層、Konva 2D、Three.js 3D、ssr:false loader 邊界)
> ⚠️  To update the auto-generated sections, architectural changes must be detected by the scanner and confirmed by a human.

## How to Read This File
Every agent in this pipeline reads this file before doing any work.
It defines the rules, patterns, and guardrails specific to this project.

## Stack Context
Next.js 16.2.10 (App Router, breaking-changes version — see warning above) + TypeScript (strict) + Tailwind CSS v4. 場地規劃器前端:Konva/react-konva(2D 平面圖 canvas)+ three/@react-three/fiber/@react-three/drei(3D 場景)。Postgres + Auth via Supabase (`@supabase/supabase-js`, `@supabase/ssr`), accessed only through our own `/api/*` route handlers. No unit/integration JS test framework installed — backend verification is manual (checklists + Insomnia collection under `supabase/tests/`); `@playwright/test` IS installed and used as the FRONTEND acceptance gate (`playwright-tests/`, page-object pattern, runs against a live dev server + the real cloud Supabase project).

## Code Style Rules
- DO use the `@/*` path alias for imports from `src/`
- DO follow `eslint-config-next` (core-web-vitals + typescript) — run lint before considering a task done
- No other conventions established yet — infer from existing code once more exists, do not invent premature patterns

## Architecture Guardrails
- Backend logic lives in Next.js API routes (App Router route handlers) — no separate backend service (per `specs/OVERVIEW.md`)
- Database: Postgres via Neon or Supabase only — never add the Vercel Postgres product
- Serverless DB connections must use the pooled connection string (`-pooler` / port 6543), not the direct one
- Never hardcode `DATABASE_URL` or any connection string — always read from environment variables

## Testing Requirements
- No unit/integration JS test framework or Docker installed. Backend verification is manual: checklists (`supabase/tests/auth_routes_manual.md`), Insomnia collection (`supabase/tests/insomnia_auth.json`), SQL verify scripts.
- For FRONTEND tasks, Playwright IS installed (`@playwright/test`, `playwright-tests/`) and is the acceptance gate (pipeline `playwright` stage) — verify criteria in a real browser using the page-object pattern (`playwright-tests/pages/`). Test accounts/credentials for Playwright runs go in `.env.playwright.local` (gitignored) — never hardcode real credentials in spec files.
- QA agent: flag if a task ships without any test coverage (manual checklist or Playwright counts) for new logic.

## Modularity Conventions
- Supabase clients live in `src/lib/supabase/`: `server.ts` (user-context, cookie-bound, RLS applies), `admin.ts` (service_role, server-only, no cookie), `middleware.ts` (proxy-context, used by `src/proxy.ts`). Never construct a Supabase client inline in a route/component — use these factories.
- API route handlers under `src/app/api/` keep validation + response logic inline (no service layer abstracted yet). Frontend must NOT call Supabase directly — go through `/api/*`.
- API auth gate: `src/proxy.ts` protects all `/api/*` by default (fail-closed) with an explicit public allowlist. New protected routes need no extra wiring; new PUBLIC routes must be added to the allowlist in `src/proxy.ts`.
- Page route protection (also `src/proxy.ts`): unauthenticated visits to protected pages redirect to `/login`; logged-in visits to auth pages (`/login`, `/register`) redirect home. New protected/auth pages need TWO updates in `src/proxy.ts`: the runtime constant (`PROTECTED_PAGES`/`AUTH_PAGES`) AND the `config.matcher` static array — Next.js analyzes `matcher` at build time, so a variable/spread reference there is silently ignored. New API routes only need the allowlist; the `/api/:path*` matcher entry already covers them.
- Client-side login-state detection lives in `src/components/AuthNav.tsx` (calls `GET /api/profile` on mount) — not in individual page components. Check there first when a spec assumes a page component owns this logic.
- Shared frontend helpers: `src/lib/auth-client.ts` (fetch wrapper for `/api/auth/*`), `src/lib/validation.ts` (client-side field validation mirroring backend rules), `src/lib/resend-cooldown.ts` (localStorage-backed cooldown timestamp helper, pattern to reuse for any future "cooldown/rate-limited action" UI).
- Payments: all payment-provider logic goes through the `PaymentProvider` adapter in `src/lib/points/provider.ts` (`createCheckout` / `verifyWebhook`) — never call a payment gateway directly from a route handler. Adding a real gateway (e.g. ECPay) means a new adapter implementation only; the checkout/point-granting flow must not change.
- Points: balances are derived (`SUM(delta)` over `point_transactions`, append-only) — never add a mutable balance column or issue UPDATEs to the ledger. Idempotency is owned by the DB (`ref_id` unique + `on conflict do nothing`), not application-level dedup. Ledger/order writes go through `admin.ts` (service_role) only; `authenticated` role has select-own RLS and no insert grant. Server-side deduction goes through `src/lib/points/ledger.ts` (`deductPoints` — typed insufficient_balance/duplicate errors) — do not hand-roll deduction inserts in routes.
- 場地規劃器分層:`src/lib/venue/`(`plan.ts` / `furniture.ts`)是**純領域模組** — 型別、常數、幾何運算,不得 import React / DOM / Konva / Three。UI 與畫布邏輯放 `src/components/venue/`。新增場地相關型別或尺寸常數一律進 `src/lib/venue/`,不要散在元件裡。
- 瀏覽器限定函式庫(Konva、Three)不可直接被 server component import — 一律透過 `*Loader.tsx`(`next/dynamic`, `ssr:false`)包一層,現有範例 `PlanEditorLoader.tsx` / `VenueSceneLoader.tsx`。新增這類重元件時比照辦理。
- 場地單位一律公尺,吸附刻度 `SNAP_M`,尺寸上限用 `VENUE_SIZE_M` / `PLAN_AREA_SIZE_M` 常數 — 不要在元件內寫死數字。家具預設尺寸/顏色/3D 高度統一由 `FURNITURE_DEFAULTS` 驅動。
- `PlanEditor.tsx` 是場地 wizard 的唯一 state owner;步驟由 `WizardStep` 型別 + `WIZARD_STEPS` 陣列驅動,新增步驟要同時更新兩者,並給步驟容器 `data-testid`(Playwright 定位靠它)。子元件(含 3D 場景)透過 props/callback 回寫,不各自持有平面圖 state。
- 步驟 03(`RefinedScene`)是**唯讀**場景:不得持有任何幾何 `useState`、不得掛 `TransformControls`、不得回寫 `onSceneChange`。它與步驟 02 讀同一份頂層 props(單一資料來源),02↔03 往返的正確性靠這點保證 —— 不要為了「快取」而在 03 存快照,那會讓資料倒退。
- 步驟 02 與 03 **互斥掛載**(一次只有一個 WebGL context),確保 GPU 資源乾淨釋放;不要改成同時掛載並用 CSS 切換。
- `AiPanel` 在步驟 03 只能用 CSS 隱藏,**不可 unmount、不可改變其在 React tree 中的掛載位置** —— 否則跨步驟對話與草稿輸入會遺失(commit `97d548c` 的成果)。
- 家具尺寸不可由使用者調整,只能移動與旋轉;尺寸唯一來源是 `FURNITURE_DEFAULTS` 的 `w`/`h`/`height3d`。不要為家具加縮放把手、不要在 AI tool schema 加 w/h 參數;匯入的外部 3D 模型一律**等比**縮放至該尺寸,不得非等比拉伸變形。
- AI (`src/lib/ai/`): all Anthropic calls go through this module (client.ts factory / system.ts frozen prompt / tools.ts strict schemas) — never construct an Anthropic client inline in a route. Every file in `src/lib/ai/` must `import "server-only"` (ANTHROPIC_API_KEY boundary). The system prompt is a frozen string — NO interpolation (dates, user ids, per-request values); prompt cache is prefix-matched and the cache_control breakpoint sits on the system block. Model/cost via env vars `AI_MODEL`/`AI_CHAT_COST`. AI routes deduct points BEFORE the model call (`reason='ai_usage'`, `refId='ai:{uuid}'`); upstream failure does not refund (phase 1 — usage log is the compensation trail). Client-supplied `system` fields are ignored, never forwarded.

## Security Rules (All Agents)
- Never hardcode secrets, tokens, or credentials
- Use environment variables for all sensitive config
- Auth approach is decided: Supabase Auth wrapped by our own API routes; session in httpOnly cookies via `@supabase/ssr`. Flag any auth-adjacent code changes immediately.
- Never log tokens/session/cookies/passwords. Keep anti-enumeration behavior on auth routes (same status + generic message regardless of account existence) — check **status code equality explicitly**, not just message text: a prior bug (register, task 2) leaked account existence via a differing status code (201 vs 200) even though the body text matched. Applies to register/login/resend and any future account-existence-adjacent endpoint.
- `service_role` key (`admin.ts`) is server-only — never import it into client components or expose to the browser.
- Public webhooks (`/api/points/webhook/*`, on the `src/proxy.ts` allowlist) have NO cookie auth — signature verification (HMAC, constant-time compare) is the sole gate. Any new webhook route must verify signatures before touching the DB and rely on DB-level idempotency (`ref_id` unique) for replay safety. Mock provider must refuse to run in production without an explicit `MOCK_PAYMENT_SECRET` (see `getPaymentProvider()`) — never weaken this guard.

## Agent-Specific Instructions

### Orchestrator
- Always check `specs/OVERVIEW.md` open questions before finalizing a task's scope — several core decisions (auth, data source, algorithm complexity) are still unresolved.

### Architect
- 複雜度熱點:`src/components/venue/PlanEditor.tsx` 已約 1584 行且是場地 wizard 的唯一 state owner — 新功能優先拆成子元件並以 props/callback 串接,不要繼續往這支檔案堆邏輯。
- 3D 效能:場景資源(貼圖、細節幾何)成本高,規劃時要明確界定在哪個步驟載入/釋放,避免拖慢 2D 編輯步驟的互動延遲。

### Developer
- Data access goes through Supabase via `src/lib/supabase/` factories — do not add another DB client/ORM ad hoc.
- Frontend must call our own `/api/*` routes, never the Supabase client directly (per story architecture decision).
- 3D 一律用既有的 `three` + `@react-three/fiber` + `@react-three/drei` 組合,不要引入其他 3D 框架或 renderer。
- 不要在 render 期間新建 geometry/material/texture 物件 — 用 `useMemo` 快取,並在卸載時 `dispose()`,否則 Three.js 會累積 GPU 資源洩漏。

### PR Reviewer
- Any change touching auth, session, or `DATABASE_URL` handling is automatically 🔴 Critical.

### QA Agent
- 核心使用者路徑:場地規劃 wizard(繪製平面圖 → 產生/預覽 3D)為主要流程,任何步驟結構變更都要驗證既有步驟行為未退化、AI 面板跨步驟常駐與 tool call 套用仍正常。
- 其餘情境仍依各 story 驗收條件推導。

---

## Workflow: Task Logging & Notion Sync (project-owned — not touched by scanner)

Every agent in this pipeline, in addition to its normal "Update State" step, must do both of the following before handing off to the next stage:

### 1. Local log — `.claude/pipeline/task-log.md`
Append one line (create the file with a header if it doesn't exist):
```
[ISO timestamp] | task: [current_task] | stage: [stage name] | agent: [agent name] | [one-line summary of what was done]
```

### 2. Notion sync — 工作進度追蹤 database (task-level granularity)
Database: https://app.notion.com/p/ada5a4b0df964a55aa38fa6669b5872a
Parent page: https://app.notion.com/p/3974eb6d721a8115ac46d73f9c664e48
Stories database (relation-linked to 工作進度追蹤 via `關聯任務` / auto reverse relation): https://app.notion.com/p/01188c7db4ff49e4ad9bae62dda71de2

**Granularity: one card per task, not per story.** Each task inside a story's 任務清單 gets its own card in 工作進度追蹤, named `[story名稱]-[task簡述]` (e.g. `會員系統-建立Supabase專案`). All task-cards belonging to the same story are related (via the `Stories` relation property) to that story's row in the Stories database.

Use the `notion-assistant` subagent to update the card matching the **current task** (not the whole story):
- If no card exists yet for this task, create one and relate it to the parent story's row in Stories database.
- Set `狀態`:
  - `scan` / `orchestrate` / `architect` / `implement` / `review` / `qa` / `playwright` (in progress) → `進行中`
  - Task fully completed (all pipeline stages done for this task) → `已完成`
  - Not yet started → `待處理`
- Set `備註` to a **short summary, max 20 characters**, describing what just happened. Examples: `完成排程API`, `審核中-無問題`, `修正bug重測`, `等待人工核准計畫`.
- Keep 備註 terse — this is a status glance, not a log. Full detail lives in `.claude/pipeline/task-log.md` and the stage's own output file (architect-plan.md, review-report.md, qa-report.md).
- When the **last** task of a story completes, also update the story's own row in the Stories database: set its `狀態` to `已完成`.

This applies at every stage transition, including pauses (architect awaiting approval, review critical-issue pause, QA escalation) — the Notion card should always reflect current reality even while the pipeline is paused for human input.

---

## Documentation Lookup (project-owned — not touched by scanner)

- **Next.js:一律讀 `node_modules/next/dist/docs/`(423 份 md)。不要查 Context7、不要憑訓練資料。** 這個 build 有 breaking changes,線上文件對它可能是錯的 —— 而且錯得很有說服力:格式正確、語氣權威、內容不符。那比沒有文件更糟。
- 其餘套件(`three` / `@react-three/fiber` / `@react-three/drei` / `konva` / `@supabase/ssr` / `tailwindcss` v4)本機只附 README,可用 Context7 MCP 查,**查的時候指定版本**(見 `package.json`)。
- Context7 是 user scope 的 MCP(`https://mcp.context7.com/mcp`),四個專案共用,匿名層每月 1000 次。

---

## 場地規劃器:第二輪定案的約束(project-owned — not touched by scanner)

第二輪回饋(`stories/venue-feedback-round2-draft.md`)實作後定案的規則。掃描器偵測不到
這些,但違反了會在很久以後才以難查的形式爆出來,所以寫在這裡。

- **尺寸標註單位一律公分**(台灣建築圖慣例),但**只有顯示層** —— 內部幾何、存檔、
  AI tool schema 全部維持公尺。新增任何標註時照 `edgeLabels()` / `columnLabel()` /
  `wallLabel()` 的既有做法轉換,不要讓公分外洩進運算。
- **牆高常數住在 `src/lib/venue/plan.ts`**,元件不得自行宣告第二份。這條有測試在守
  (`venue-wall-height` 案例7 直接 grep `src/components/`),因為它原本就是硬編兩份
  才被抓出來的。
- **步驟 02 與 03 共用同一份家具幾何**(`useNormalizedFurnitureModel` / 程序化模組層
  快取),差別只在材質:02 是單色、零貼圖,03 才上材質。不要為了某一步「方便」而
  複製一份幾何,那會讓跨步驟快取失效,也讓兩個畫面慢慢長歪。
- **探針(`data-*`)必須回報場景/GPU 的實際狀態,不能是 prop 的回音。** 第二輪有
  三次「測試全綠但實作已經壞掉」,全是守衛在讀自己傳進去的值。要斷言幾何就量包圍盒、
  要斷言材質就從 GPU 讀回烘焙結果,不要讀那個剛剛才傳下去的 prop。
- **重量級 3D 測試要明確編列 timeout 預算。** 沒有 GPU 的環境(CI 容器走 SwiftShader
  軟體算圖)比開發機慢 3–4 倍,壓在預設 30s 邊緣的測試會偽裝成隨機 flake —— 同一份
  程式換台機器就翻面,查起來極耗時。往返多趟或拍截圖的測試一律 `test.slow()`,更重的
  用 `test.setTimeout()` 並在註解寫下實測耗時。現有範例:`venue-procedural-furniture`
  的 P6 / P8、`venue-refined-lighting` 案例12 / 14、`venue-refined-materials` T14。
